### Pedestrian model
In this folder there is scripts and functions to create pedestrian parameters for the use in the simulation, testing live scripts and functions.

The idea is that you create a `pedestriam_params.mat` file so the bridge+pedestrian(interaction) model uses the data to simulate.
