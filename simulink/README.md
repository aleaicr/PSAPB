# Simulinks
This folder contains simulinks for each desired crowd size.

The reason why multiple simulinks are needed is because the simulink model doesn't work with a predefined number of pedestrians. So each simulink is a copy of the original one, but with a different number of pedestrians.